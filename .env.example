# 公開 URL（metadata / sitemap の絶対 URL 生成に使う）。Vercel では本番ドメインを設定する
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# 002-instagram-connect
# 連携プロバイダ: stub | meta（既定 stub）。
# 本アプリはサーバー側に状態を持たない構成のため、実 Meta 連携（meta）はトークン交換用の
# サーバー処理を追加するまで未実装（設定すると起動時エラー）。
NEXT_PUBLIC_INSTAGRAM_PROVIDER=stub
