# 依存パッケージの任意スクリプト実行を禁止する（サプライチェーン攻撃対策）。
# 必要なネイティブバイナリは postinstall に依存せず Dockerfile で明示的に配置する。
ignore-scripts=true
min-release-age=3
# engines フィールドを強制する（volta 非利用者が古い Node で install するのを fail-fast で防ぐ）
engine-strict=true
